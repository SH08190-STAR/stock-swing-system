# Toss Relay 전용 runtime 의존성 — Streamlit requirements와 분리 유지.
# 아래 5개는 main.py/config.py/Dockerfile이 직접 사용하는 dependency이며,
# 로컬에서 tests/test_toss_relay.py 49건을 통과한 상호 호환 조합으로 고정한다.
# (Streamlit·Supabase·pandas·numpy·pyarrow 등은 Relay에 불필요 — 미포함.)
# 테스트 전용(pytest·httpx)은 여기 넣지 않고 CI에서 별도 설치한다.
fastapi==0.139.0
starlette==1.3.1
uvicorn==0.49.0
pydantic==2.13.4
requests==2.34.2
